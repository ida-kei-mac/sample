# 試験結果報告書(test-report.md)

## 実施概要

| 項目 | 内容 |
|---|---|
| 対象 | APIキー発行台帳 ステップ1「発行登録フォーム+入力検証」 |
| 実施日 | 2026-07-09 |
| 実行コマンド | `pytest -v --junitxml=artifacts/result.xml` |
| 実行環境 | Windows 11 / Python 3.12.10 / PySide6 6.11.1 / pytest 9.1.1 + uiautomation |
| 起動条件 | `python -m app --test`(一時SQLite、ウィンドウ位置サイズ固定)、`APP_FAKE_NOW=2026-07-09T12:00:00` |
| 結果 | **32件中32件 合格**(失敗0件、実行時間 約69秒) |
| 詳細設計 | [test-design.md](./test-design.md)(ケースID・REQ-ID対応)/ JUnit XML: `artifacts/result.xml` |

## 試験結果一覧

| 項目 | 試験方法 | 判定 |
|---|---|---|
| TC-001 利用者名欄の初期状態 | 起動直後に利用者名入力欄のテキストを取得し、空文字であることを確認 | ✅ 合格 |
| TC-002 部署コンボの初期状態 | 部署コンボボックスの項目一覧と選択中項目を取得し、「開発部・営業部・管理部」の3件・この順序・初期選択「開発部」であることを確認 | ✅ 合格 |
| TC-003 予算上限欄の初期状態 | 起動直後に予算上限入力欄のテキストを取得し、空文字であることを確認 | ✅ 合格 |
| TC-004 ステータス欄の初期状態 | 起動直後にステータス欄のテキストを取得し、空文字であることを確認 | ✅ 合格 |
| TC-005 発行ボタンの初期状態 | 起動直後に[発行]ボタンの活性状態を取得し、クリック可能であることを確認 | ✅ 合格 |
| TC-006 部署が常に選択済み | 部署で「営業部」を選択し、選択中項目が「営業部」となり未選択にならないことを確認 | ✅ 合格 |
| TC-007 半角空白のトリム | 利用者名「␣田中␣」・予算「␣100␣」で発行し、成功メッセージ表示と、トリム後の値(田中/100)でDB保存されることを確認 | ✅ 合格 |
| TC-008 空白のみの利用者名 | 利用者名に半角空白のみを入力して発行し、「利用者名を入力してください」表示・非保存を確認 | ✅ 合格 |
| TC-009 文字数のUnicodeカウント | 全角25+半角25の計50文字の利用者名で発行し、成功すること(全角半角を区別しない)を確認 | ✅ 合格 |
| TC-010 検証順序(先頭エラーのみ) | 利用者名・予算とも空のまま発行し、利用者名のエラーのみが表示されることを確認 | ✅ 合格 |
| TC-011 利用者名の未入力 | 利用者名空・予算100で発行し、「利用者名を入力してください」表示・非保存を確認 | ✅ 合格 |
| TC-012 利用者名51文字(境界値NG) | 51文字の利用者名で発行し、「利用者名は50文字以内で入力してください」表示・非保存を確認 | ✅ 合格 |
| TC-013 利用者名50文字(境界値OK) | 50文字の利用者名で発行し、成功メッセージが表示されることを確認 | ✅ 合格 |
| TC-014 予算上限の未入力 | 予算空のまま発行し、「予算上限を入力してください」表示・非保存を確認 | ✅ 合格 |
| TC-015 予算が非数値 | 予算「abc」で発行し、「予算上限は整数で入力してください」表示・非保存を確認 | ✅ 合格 |
| TC-016 予算が負数 | 予算「-5」で発行し、「予算上限は整数で入力してください」表示・非保存を確認 | ✅ 合格 |
| TC-017 予算が全角数字 | 予算「１００」で発行し、「予算上限は整数で入力してください」表示・非保存を確認 | ✅ 合格 |
| TC-018 予算が小数 | 予算「1.5」で発行し、「予算上限は整数で入力してください」表示・非保存を確認 | ✅ 合格 |
| TC-019 予算がカンマ区切り | 予算「1,000」で発行し、「予算上限は整数で入力してください」表示・非保存を確認 | ✅ 合格 |
| TC-020 予算0(下限-1、境界値NG) | 予算「0」で発行し、「予算上限は1〜1,000,000の範囲で入力してください」表示・非保存を確認 | ✅ 合格 |
| TC-021 予算1(下限、境界値OK) | 予算「1」で発行し、成功メッセージが表示されることを確認 | ✅ 合格 |
| TC-022 予算1,000,000(上限、境界値OK) | 予算「1000000」で発行し、成功メッセージが表示されることを確認 | ✅ 合格 |
| TC-023 予算1,000,001(上限+1、境界値NG) | 予算「1000001」で発行し、「予算上限は1〜1,000,000の範囲で入力してください」表示・非保存を確認 | ✅ 合格 |
| TC-024 予算の先頭ゼロ | 予算「007」で発行し、成功メッセージ表示と整数値7でDB保存されることを確認 | ✅ 合格 |
| TC-025 エラー時の入力保持 | 51文字の利用者名・部署「営業部」・予算100で発行してエラーを起こし、全入力欄の値が変化しないことを確認 | ✅ 合格 |
| TC-026 エラー時の既存レコード保全 | 1件発行成功後に予算「0」でエラーを起こし、レコード数が1のまま・既存レコードのキーが不変であることを確認 | ✅ 合格 |
| TC-027 APIキーの形式 | 発行成功後のステータス欄からキーを抽出し、`sk-[a-z0-9]{32}` 形式に完全一致することを確認 | ✅ 合格 |
| TC-028 APIキーの一意性 | 同条件で2回発行し、2つのキーが互いに異なり、DB上の2レコードのキーも異なることを確認 | ✅ 合格 |
| TC-029 保存レコードの内容 | 利用者名「田中」・部署「管理部」・予算500で発行し、DBに利用者名・部署・予算(整数)・キー・発行日時(APP_FAKE_NOWの値)が保存されることを確認 | ✅ 合格 |
| TC-030 同名での再発行 | 同じ利用者名で2回発行し、2回とも成功しレコードが2件保存されることを確認 | ✅ 合格 |
| TC-031 成功メッセージの文言 | 発行成功時のステータス欄が「発行しました: <キー>」に完全一致することを確認 | ✅ 合格 |
| TC-032 成功後の入力保持 | 発行成功後に利用者名・部署・予算の各入力欄の値が変化しないことを確認 | ✅ 合格 |

## 特記事項

- すべての期待文言は [requirements.md](./requirements.md)「エラーメッセージ一覧」(MSG-001〜006)との完全一致で判定した。
- 「保存されない/される」の確認は、`--test` 起動時に作成される一時SQLiteファイルを直接参照して行った。
- 実施過程でテストコード側の不具合(UIAロケータの照合方法、コンボボックス選択の操作パターン)を2回修正して再実行した。アプリ側の不具合は検出されなかった。
